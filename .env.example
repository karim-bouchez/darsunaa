# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to \`.env\`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL database connection URL for application data (NOT auth tables)
# Format: postgres://[USERNAME]:[PASSWORD]@[HOST]:[PORT]/[DATABASE]
# Example for local PostgreSQL: postgres://karim:password@localhost:5432/darsunaa
# Note: Authentication tables are managed by the auth-service microservice
POSTGRES_URL="postgres://username:password@localhost:5432/database_name"

# ============================================================================
# AUTHENTICATION SERVICE CONFIGURATION
# ============================================================================
# URL of the external auth-service microservice
# For Next.js: Use localhost since it runs on the same machine
NEXT_PUBLIC_AUTH_SERVICE_URL="http://localhost:3001"

# For Expo: Use your local IP address (find it with `ifconfig` on Mac/Linux or `ipconfig` on Windows)
# Development example: EXPO_PUBLIC_AUTH_SERVICE_URL="http://192.168.1.10:3001"
# Production example: EXPO_PUBLIC_AUTH_SERVICE_URL="https://auth.yourdomain.com"
EXPO_PUBLIC_AUTH_SERVICE_URL="http://192.168.1.10:3001"

# ============================================================================
# API CONFIGURATION (FOR EXPO ONLY)
# ============================================================================
# URL of the Next.js tRPC API (Expo app only - Next.js doesn't need this)
# Development: Use your local IP address with port 3000
# Production: Your deployed Next.js URL
# Example: EXPO_PUBLIC_API_URL="http://192.168.1.10:3000"
EXPO_PUBLIC_API_URL="http://192.168.1.10:3000"